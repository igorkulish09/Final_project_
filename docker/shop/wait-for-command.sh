#!/bin/bash

# Очікуємо доступність бази даних Postgres перед запуском Django
until PGPASSWORD=shop_password psql -h "db" -U "shop_user" -d "shop_db" -c '\q'; do
  echo "База даних недоступна - чекаємо..."
  sleep 2
done

# Інші очікувані команди, які можуть бути додані
# Наприклад, очікування доступності Redis або інших служб.

# Після успішного очікування можемо продовжити з іншими діями або запустити Django.
exec "$@"
